const assert = require("assert");

describe('Basic Test', function() {
  describe('First Test', function() {
    it("should test the most basic equaltiy test", function() {
      assert.equal(1, 1);
    })
  })
});
